name: inference
device: cuda
gpus: [0]
vis_result: True
vis_hm: True
vis_traj: False
model_path: 
eval:
  score_threshold: 0.5
visualization:
  show_speed_direction: True
  speed_window_frames: 4
  direction_window_frames: 1
  speed_smoothing_alpha: 0.35
  direction_change_threshold_deg: 110
  direction_min_distance_m: 0.01
  direction_stabilization_frames: 1
  hud_position: top_center
