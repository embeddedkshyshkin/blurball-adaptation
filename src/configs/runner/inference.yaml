name: inference
device: cuda
gpus: [0]
vis_result: True
vis_hm: True
vis_traj: False
model_path: 
eval:
  score_threshold: 0.5
visualization:
  show_speed_direction: True
  speed_window_frames: 4
  speed_smoothing_alpha: 0.35
  hud_position: top_center
